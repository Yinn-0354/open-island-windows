<Window x:Class="OpenIsland.App.Views.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenIsland.App"
        mc:Ignorable="d"
        Title="Open Island"
        Width="320"
        Height="400"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:StringToBrushConverter x:Key="StringToBrushConverter"/>
    </Window.Resources>

    <Border Background="#1E1E1E"
            BorderBrush="#3E3E3E"
            BorderThickness="1"
            CornerRadius="8"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#2D2D2D" CornerRadius="8,8,0,0" Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="🏝️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Open Island"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#FFFFFF"/>
                    </StackPanel>

                    <TextBlock Grid.Column="1"
                               Text="🔴"
                               FontSize="12"
                               Visibility="{Binding HasAttentionSessions, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>

            <!-- 状态栏 -->
            <Border Grid.Row="1" Background="#252525" Padding="12,6">
                <TextBlock Text="{Binding AttentionCount, StringFormat='{}⚠️ {0} 个会话需要关注'}"
                           Foreground="#FF9800"
                           FontSize="11"
                           Visibility="{Binding HasAttentionSessions, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Border>

            <!-- 会话列表 -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="8">
                <ItemsControl ItemsSource="{Binding Sessions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2D2D2D"
                                    CornerRadius="6"
                                    Padding="10"
                                    Margin="0,2"
                                    MouseLeftButtonDown="SessionItem_Click">
                                <Border.InputBindings>
                                    <MouseBinding Gesture="LeftClick" Command="{Binding ClickCommand}"/>
                                </Border.InputBindings>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 工具图标和状态 -->
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding ToolIcon}" FontSize="18"/>
                                        <TextBlock Text="{Binding StatusIndicator}" FontSize="10" HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- 会话信息 -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="#FFFFFF"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Summary}"
                                                   FontSize="10"
                                                   Foreground="#AAAAAA"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <!-- 快捷操作 -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="✓"
                                                Visibility="{Binding ShowApproveButton, Converter={StaticResource BoolToVisibilityConverter}}"
                                                Command="{Binding ApproveCommand}"
                                                Background="#4CAF50"
                                                Foreground="White"
                                                Width="24" Height="24"
                                                Margin="0,0,4,0"
                                                Click="ApproveButton_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 底部按钮 -->
            <Border Grid.Row="3" Background="#2D2D2D" CornerRadius="0,0,8,8" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="控制中心"
                            Command="{Binding OpenControlCenterCommand}"
                            Background="Transparent"
                            Foreground="#AAAAAA"
                            Padding="8,4"/>

                    <Button Grid.Column="1" Content="设置"
                            Command="{Binding OpenSettingsCommand}"
                            Background="Transparent"
                            Foreground="#AAAAAA"
                            Padding="8,4"/>

                    <Button Grid.Column="2" Content="退出"
                            Command="{Binding ExitCommand}"
                            Background="Transparent"
                            Foreground="#F44336"
                            Padding="8,4"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
